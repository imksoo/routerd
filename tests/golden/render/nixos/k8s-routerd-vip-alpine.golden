### routerd-generated.nix
# Generated by routerd. Do not edit by hand.
{ config, lib, pkgs, ... }:

{
  networking.useDHCP = false;
  networking.useNetworkd = true;
  networking.firewall.checkReversePath = false;
  systemd.network.enable = true;
  systemd.network.networks."10-netplan-eth0" = {
    matchConfig.Name = "eth0";
    address = [
      "192.168.1.38/24"
    ];
    linkConfig.RequiredForOnline = "routable";
  };
  systemd.services."routerd-dns-resolver@vip-resolver" = {
    description = "routerd DNS resolver vip-resolver";
    wantedBy = [ "multi-user.target" ];
    after = [ "network-online.target" ];
    wants = [ "network-online.target" ];
    serviceConfig = {
      ExecStart = lib.concatStringsSep " " [
        "/usr/local/sbin/routerd-dns-resolver"
        "daemon"
        "--resource"
        "vip-resolver"
        "--config-file"
        "/var/lib/routerd/dns-resolver/vip-resolver/config.json"
      ];
      Restart = "always";
      RestartSec = "5s";
      RuntimeDirectory = [ "routerd/dns-resolver" ];
      StateDirectory = [ "routerd/dns-resolver" "routerd/dns-resolver/vip-resolver" ];
      LogsDirectory = [ "routerd" ];
      ReadWritePaths = [ "/run/routerd" "/var/lib/routerd" "/var/log/routerd" ];
      AmbientCapabilities = [ "CAP_NET_BIND_SERVICE" ];
      CapabilityBoundingSet = [ "CAP_NET_BIND_SERVICE" ];
      RestrictAddressFamilies = [ "AF_UNIX" "AF_INET" "AF_INET6" ];
      ProtectSystem = "strict";
      ProtectHome = "yes";
      NoNewPrivileges = true;
      PrivateTmp = true;
    };
  };
}
