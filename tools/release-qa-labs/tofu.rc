provider_installation {
  filesystem_mirror {
    path    = "/var/lib/routerd-release-qa/provider-mirror"
    include = ["registry.opentofu.org/*/*"]
  }

  direct {
    exclude = ["registry.opentofu.org/*/*"]
  }
}
